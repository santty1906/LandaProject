[pytest]
testpaths = tests
python_files = test_*.py
addopts =
    -ra
    --strict-config
    --cov=app
    --cov-branch
    --cov-report=term-missing:skip-covered
    --cov-report=xml
    --cov-fail-under=70
filterwarnings =
    error::DeprecationWarning:app.*
