# Backend (FastAPI)

FastAPI scaffold for authentication and banking APIs.

## Planned modules

- `app/api/v1`: HTTP routes
- `app/application`: use cases/services
- `app/domain`: entities and interfaces
- `app/infrastructure`: database and adapters
- `app/core`: config, logging, exceptions
- `tests`: unit/integration/api test structure

No business logic is implemented yet.
