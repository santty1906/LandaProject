# Architecture Overview (Setup Stage)

This repository uses a modular monorepo:

- Flutter frontend for mobile client
- FastAPI backend for REST API
- PostgreSQL for persistence
- Docker for local environment consistency

Current state: folder and tooling foundations only.
